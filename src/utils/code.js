const code = `Install into your project using a node package manager.

Using npm:

<p className='code-snippet'>npm install react-pure-loaders</p>

Using yarn:

<p className='code-snippet'>yarn add react-pure-loaders</p>

Import into the component:

<p className='code-snippet'>import { BallBeat } from 'react-pure-loaders';</p>

Create the component:

`;

export default code;
